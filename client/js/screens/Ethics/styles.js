import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    fontWeight: '900',
    alignSelf: 'center',
    marginBottom: 30,
    marginTop: 10,
  },
});

export default styles;
